# This is the service that runs your Telegram bot
[start]
command = "python bot.py"

# Environment variables (Railway fills TELEGRAM_TOKEN in dashboard)
[variables]

# Cron job to run your scraper every hour
[[cron]]
schedule = "0 * * * *"
command = "python main.py"
